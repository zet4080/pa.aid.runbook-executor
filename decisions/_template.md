---
key:
story:
status: pending
selected_approach:
decided_by:
decided_at:
---

<!-- FRONTMATTER INSTRUCTIONS
Fill in the fields above:
  key:               Jira issue key, e.g. ARC-1346
  story:             One-line human-readable title for the decision
  status:            Set to `pending` initially; change to `answered` when you have chosen an approach
  selected_approach: Leave blank until you decide; fill in `Option N` (e.g. Option 1) when answering
  decided_by:        Your name or handle
  decided_at:        ISO date you answered, e.g. 2026-07-01

When you are ready to unblock the agent:
  1. Set `status: answered`
  2. Set `selected_approach: Option N`
  3. Commit and push — the agent will detect `status: answered` on resume and proceed automatically
-->

## Decision Context

<!-- Describe the context that makes this decision necessary:
  - What is the story trying to achieve?
  - What constraint or trade-off forced a choice?
  - Why can't one approach be obviously picked without human input?
-->

## Options

<!-- Use the format below for each option. The agent populates this section.
Option N (recommended): {name}
  How: {1-2 sentences describing the approach}
  Pro: {key advantage}
  Con: {key trade-off}
-->

Option 1 (recommended): {name}
  How:
  Pro:
  Con:

Option 2: {name}
  How:
  Pro:
  Con:

Option 3: {name}
  How:
  Pro:
  Con:

Recommendation: Option N because {reason}.

## Recommendation

<!-- Write a brief rationale for your selection. Reference the Option number you chose in
`selected_approach` above. For example:
  "Choosing Option 2 because it avoids introducing a new dependency and fits the existing pattern."
-->

## Notes

<!-- Optional: any additional context, constraints discovered after the decision was made,
or instructions to the agent about how to proceed. -->
